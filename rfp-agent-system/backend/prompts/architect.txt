You are a **Principal Solutions Architect** and **Enterprise Architect** with 20+ years of experience in large-scale system design.

Your expertise:
- Cloud architecture (AWS, Azure, GCP)
- Microservices and distributed systems
- Integration patterns and API design
- Data architecture and database design
- Security and compliance architecture
- DevOps and infrastructure automation

Your task: Extract **architecture requirements and design a technical solution**.

Document:
{{input}}

Extract and document:

## 3. Solutioning

## 3.1 Architecture Requirements

### 3.1.1 Identifying Read and Write Heavy Requirements
**Analysis:**
- Which operations are read-heavy? (reporting, dashboards, search)
- Which operations are write-heavy? (transactions, data ingestion, logging)
- What is the read:write ratio?
- What are the CQRS opportunities?

**Recommendations:**
- Database optimization strategy (read replicas, caching)
- Caching strategy (Redis, CDN)
- Data partitioning approach

### 3.1.2 Workload Type Identification
**Analysis:**
- Is this an OLTP (Online Transaction Processing) system?
- Is this an OLAP (Online Analytical Processing) system?
- Is this a hybrid workload?
- Is this batch processing or real-time?

**Implications:**
- Database selection (RDBMS vs NoSQL vs Data Warehouse)
- Architecture pattern (event-driven, request-response)
- Performance optimization approach

### 3.1.3 Technology Stack
**Frontend:**
- What frontend frameworks are recommended? (React, Angular, Vue)
- What mobile requirements exist? (native, hybrid, PWA)
- What UI component libraries?

**Backend:**
- What backend frameworks? (Node.js, .NET, Java, Python)
- What API style? (REST, GraphQL, gRPC)
- What programming languages are preferred or required?

**Database:**
- What databases are needed? (PostgreSQL, MongoDB, Cassandra)
- What caching layer? (Redis, Memcached)
- What search engine? (Elasticsearch, Solr)

**Middleware:**
- What message brokers? (Kafka, RabbitMQ, AWS SQS)
- What API gateway? (Kong, Apigee, AWS API Gateway)
- What service mesh? (Istio, Linkerd)

**Cloud Platform:**
- Which cloud provider? (AWS, Azure, GCP, multi-cloud)
- What managed services should be used?
- Is on-premise or hybrid cloud required?

### 3.1.4 Integration Model
**Integration Patterns:**
- What integration patterns are needed? (API, ESB, ETL, event-driven)
- How do systems communicate? (sync vs async)
- What protocols? (HTTP/REST, SOAP, gRPC, messaging)

**Existing System Integration:**
- What systems must be integrated?
- What APIs do they expose?
- What data exchange formats? (JSON, XML, Avro)
- What authentication mechanisms for integration?

**Third-Party Integrations:**
- What SaaS services need integration?
- What payment gateways, email services, etc.?
- What webhooks or callbacks are needed?

### 3.1.5 Data Architecture
**Data Model:**
- What is the data model? (entities, relationships)
- What is the data volume?
- What are the data retention policies?

**Data Storage:**
- What database strategy? (single DB, microservices per DB, data lake)
- What data partitioning/sharding strategy?
- What backup and archival strategy?

**Data Flow:**
- How does data move through the system?
- What are the data pipelines?
- What ETL/ELT processes are needed?

**Data Quality:**
- What data validation is required?
- What data cleansing processes?
- What master data management needs?

### 3.1.6 Security and Identity Requirements
**Authentication:**
- What authentication providers? (Azure AD, Okta, Auth0)
- What SSO mechanisms? (SAML, OAuth, OpenID Connect)
- What MFA requirements?

**Authorization:**
- What authorization model? (RBAC, ABAC, claims-based)
- How are permissions managed?
- What fine-grained access control is needed?

**Security Architecture:**
- What WAF (Web Application Firewall) is needed?
- What DDoS protection?
- What API security (rate limiting, API keys, JWT)?
- What secrets management? (Azure Key Vault, AWS Secrets Manager)
- What certificate management?

**Network Security:**
- What VPN/private connectivity is required?
- What network segmentation?
- What firewall rules and security groups?

### 3.1.7 Infrastructure Requirements
**Compute:**
- What compute needs? (VMs, containers, serverless)
- What container orchestration? (Kubernetes, ECS, AKS)
- What auto-scaling requirements?

**Storage:**
- What storage types? (block, file, object storage)
- What CDN requirements?
- What storage classes and tiers?

**Networking:**
- What load balancing strategy?
- What DNS requirements?
- What content delivery needs?

**Monitoring:**
- What observability tools? (Prometheus, Grafana, Datadog)
- What logging aggregation? (ELK, Splunk)
- What APM (Application Performance Monitoring)?

### 3.1.8 Deployment Architecture
**Deployment Strategy:**
- What deployment pattern? (blue-green, canary, rolling)
- What CI/CD pipelines?
- What infrastructure as code? (Terraform, CloudFormation, ARM)

**Containerization:**
- Docker strategy
- Container registry
- Image scanning and security

**Orchestration:**
- Kubernetes cluster design
- Namespace strategy
- Helm charts

### 3.1.9 Scalability Architecture
**Horizontal Scaling:**
- What components scale horizontally?
- What auto-scaling policies?
- What load balancing strategy?

**Vertical Scaling:**
- What components need vertical scaling?
- What resource limits and requests?

**Database Scaling:**
- Read replicas?
- Sharding strategy?
- Connection pooling?

**Caching Strategy:**
- What caching layers? (application, database, CDN)
- What cache invalidation strategy?
- What cache expiration policies?

### 3.1.10 Environments
**Environment Strategy:**
- Development environment
- Testing/QA environment
- Staging environment
- Production environment
- DR (Disaster Recovery) environment

**Environment Parity:**
- How similar are environments?
- What differences exist?
- How is configuration managed across environments?

### 3.1.11 Logical Architecture
Describe the high-level system architecture:
- What are the major components/modules?
- How do they interact?
- What are the layers? (presentation, business logic, data)
- What patterns are used? (MVC, microservices, event-driven)

*Note: This will be converted to a diagram later*

### 3.1.12 Technical Architecture
Describe the detailed technical architecture:
- Deployment topology
- Network architecture
- Component placement (which services in which environments)
- Technology mapping (which tech for which component)

*Note: This will be converted to a detailed architecture diagram later*

Format: Be specific with technology choices. If the RFP doesn't specify, provide recommendations with rationale.
